<DataTemplate
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
	xmlns:local="clr-namespace:WirelessADBManagerVSExtension"
	xmlns:models="clr-namespace:WirelessADBManagerVSExtension.Models"
	xmlns:qr="clr-namespace:QRCoder.Xaml;assembly=QRCoder.Xaml"
	xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
	xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
	DataType="local:WirelessADBManagerToolWindowData">

	<DataTemplate.Resources>
		<Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" TargetType="Label" />
		<Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}" TargetType="TextBox" />
		<Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" TargetType="Button" />
		<Style TargetType="TextBlock">
			<Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
		</Style>
		<Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogListViewStyleKey}}" TargetType="ListView" />
		<BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</DataTemplate.Resources>

	<Grid Margin="50">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<StackPanel Grid.Column="0" Margin="0,0,25,0">
			<Border
				x:Name="QRImage"
				Width="250"
				Height="250"
				Padding="20"
				HorizontalAlignment="Center"
				Background="White">
				<Path
					Data="{Binding QrDataSvg}"
					Fill="Black"
					Stretch="Uniform" />
			</Border>
			<TextBlock
				MaxWidth="{Binding ElementName=QRImage, Path=ActualWidth}"
				Margin="0,20,0,0"
				HorizontalAlignment="Center"
				VerticalAlignment="Center"
				Text="Scan the QR code from your phone’s Developer Settings under Wireless Debugging"
				TextAlignment="Center"
				TextWrapping="Wrap" />
		</StackPanel>

		<StackPanel Grid.Column="1" Margin="25,0,0,0">
			<Label Content="Discovering..." FontSize="24" />
			<ProgressBar Margin="0,5,0,10" IsIndeterminate="True" />
			<ListView
				Margin="0,10,0,0"
				VerticalAlignment="Stretch"
				HorizontalContentAlignment="Stretch"
				VerticalContentAlignment="Stretch"
				ItemsSource="{Binding Devices}">
				<ListView.ItemTemplate>
					<DataTemplate DataType="models:DeviceInfo">
						<Grid Margin="5">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="1*" />
								<ColumnDefinition Width="2*" />
								<ColumnDefinition Width="2*" />
								<ColumnDefinition Width="2*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>
							<Image
								Grid.Column="0"
								Width="40"
								Height="40"
								HorizontalAlignment="Center"
								VerticalAlignment="Center"
								Source="{Binding DeviceIconUri}"
								Stretch="Fill" />
							<Label
								Grid.Column="1"
								HorizontalAlignment="Center"
								VerticalAlignment="Center"
								Content="{Binding Model}" />
							<Label
								Grid.Column="2"
								HorizontalAlignment="Center"
								VerticalAlignment="Center"
								Content="{Binding Ip}" />
							<Button
								Grid.Column="3"
								Padding="10"
								VerticalAlignment="Center"
								Command="{Binding ActionCommand}"
								CommandParameter="{Binding .}"
								Content="{Binding StateText}" />
							<Button
								Grid.Column="4"
								Margin="6,0,0,0"
								Padding="10"
								VerticalAlignment="Center"
								Command="{Binding TurnOffWirelessCommand}"
								CommandParameter="{Binding .}"
								Content="Turn off wireless ADB"
								Visibility="{Binding ShowTurnOffWirelessButton, Converter={StaticResource BoolToVisibilityConverter}}" />
						</Grid>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>

		</StackPanel>

	</Grid>
</DataTemplate>
