namespace WirelessADBManagerVSExtension.Models;

public enum ServiceMode
{
    Pair,
    Pairing,
    Connect
}

public enum DeviceStates
{
    ManualPair,
    Pairing,
    Connecting,
    Connected,
    Disconnected
}
